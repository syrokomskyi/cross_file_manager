name: cross_file_manager
description: Can read files from assets, Internet, zip archives by loader priority.
homepage: https://github.com/signmotion/cross_file_manager

version: 0.1.0+100


environment:
  flutter: '>=2.8.1 <3.0.0'
  sdk: '>=2.15.1 <3.0.0'

dependencies:
  archive: ^3.1.8
  flutter:
    sdk: flutter
  path: ^1.8.0
  path_provider: ^2.0.8

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
